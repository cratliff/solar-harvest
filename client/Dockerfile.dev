FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
# src/ is bind-mounted at runtime for hot reload — COPY here is for image-only builds
COPY . .
EXPOSE 4200
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--proxy-config", "proxy.conf.docker.json", "--poll", "2000"]
